# Campaign CMS - Codespaces Environment

# Environment Configuration
NODE_ENV=development
PORT=3001

# Frontend Configuration  
FRONTEND_PORT=3000

# Database Configuration
DB_PATH=./data/campaign_cms.sqlite

# API Configuration (will be auto-configured for Codespaces)
API_BASE_URL=/api

# CORS Configuration (allow all origins in Codespaces)
FRONTEND_URL=*

# Logging Configuration
LOG_LEVEL=info

# Note: CODESPACE_NAME and GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN
# are automatically set by GitHub Codespaces
