// Channels removed from product; export empty placeholders to avoid import errors.
export type ChannelKey = never;
export type FieldSpec = never;
export type ChannelMeta = never;
export const CHANNEL_CONFIGS: Record<string, never> = {};
